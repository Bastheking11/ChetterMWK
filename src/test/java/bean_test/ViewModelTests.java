package bean_test;

import test_resource.UserViewTest;
import domain.entity.User;
import domain.viewmodel.ViewModel;
import org.junit.Assert;
import org.junit.Test;

public class ViewModelTests {

//    @Test
//    public void users() {
//        User user = new User("m@t.nl", "test", "Martijn", true);
//        UserViewTest view = ViewModel.Convert(user, UserViewTest.class);
//
//        Assert.assertNotNull("View object should been created", view);
//        Assert.assertEquals("Username should have been taken", user.getUsername(), view.getUsername());
//        Assert.assertNotEquals("Administator should been overwritten", user.isAdministrator(), view.isAdministrator());
//        Assert.assertEquals("Email gets passed by setter", "m", view.getEmail());
//    }

}
